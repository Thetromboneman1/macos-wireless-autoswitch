name: Workflow Health Audit

on:
  schedule:
    - cron: "42 12 * * 1"
  workflow_dispatch:
  pull_request:
    paths:
      - ".github/workflows/**"
      - "docs/github-actions/**"
      - "docs/repository-governance/**"
      - "docs/prompts/**"
      - "scripts/platform-modernization/**"

permissions:
  contents: read

concurrency:
  group: workflow-health-audit-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install validation tools
        shell: bash
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y shellcheck
          python -m pip install --upgrade pip
          python -m pip install yamllint

      - name: GitHub Actions syntax
        uses: rhysd/actionlint@v1.7.12

      - name: YAML configuration
        shell: bash
        run: yamllint .github/workflows/*.yml .yamllint

      - name: Shell scripts
        shell: bash
        run: shellcheck install.sh wireless.sh scripts/**/*.sh

      - name: Markdown documentation
        shell: bash
        run: npx --yes markdownlint-cli@0.45.0 README.md docs/**/*.md

      - name: Secret scan
        uses: gitleaks/gitleaks-action@v3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
