# Third-Party AI Tooling Audit

Date: 2026-07-11

This report is generated by `scripts/platform-modernization/install-third-party-ai-tooling.sh`.

## Current Decision

The local toolchain should absorb reusable, licensed agent knowledge while preserving the working local AI architecture. oMLX remains the default OpenAI-compatible endpoint, and new router or runtime projects stay disabled until a measured compatibility review proves they improve the current setup.

## Repository Classifications

| Repository | Status | Rationale |
|---|---|---|
| `mukul975/Anthropic-Cybersecurity-Skills` | Installed | Apache-2.0 structured agent skills. Installed into local skill roots with `cybersecurity-` directory prefixes to avoid overwriting existing skills. |
| `asgeirtj/system_prompts_leaks` | Audit-only | Contains leaked system-prompt material. Cloned for metadata review only; not copied into prompts, skills, model configs, or committed docs. |
| `decolua/9router` | Lab candidate | MIT router/gateway that wants to set `OPENAI_BASE_URL` to its own endpoint. Not wired by default because it would conflict with the oMLX-first routing contract. |
| `Osmantic/ODS` | Lab candidate | Apache-2.0 local AI platform stack. Not started or installed because it overlaps the existing Boneman local-AI platform and may expose services or change Docker/runtime state. |
| `itsfatduck/optimizerDuck` | Audit-only | Windows optimization app. Not applicable to this Apple Silicon macOS toolchain beyond reusable prompt/skill ideas. |
| `inkeep/open-knowledge` | Lab candidate | GPL-3.0 knowledge editor/wiki. Cloned for review; no replacement of the existing llm-wiki/Open Knowledge setup during this pass. |
| `JustVugg/colibri` | Retired | The experiment failed the promotion gate. Its unused model tree and active selectors were deleted on 2026-08-18. Port `8020` must stay closed and automation must not redownload it. |
| `tirth8205/code-review-graph` | Verified candidate | MIT MCP/code graph tool. Kept as a candidate alongside the already active `codebase-memory-mcp`; no config replacement without measured benefit. |

## Installed Skill Roots

The cybersecurity skills loop writes symlinked, namespaced skill directories into any existing local roots:

- `~/.codex/skills`
- `~/.agents/skills`
- `~/.claude/skills`
- `~/.config/goose/skills`
- `~/.hermes/skills`

Each installed directory is named `cybersecurity-<upstream-skill-name>` and points back to the checked-out source under `/Users/corn/Documents/Boneman_Projects/local-ai-platform/third-party-tool-audit/sources/Anthropic-Cybersecurity-Skills/skills`.

## Validation Notes

Run:

```bash
scripts/platform-modernization/install-third-party-ai-tooling.sh
actionlint .github/workflows/*.yml
shellcheck install.sh wireless.sh scripts/**/*.sh
markdownlint README.md docs/**/*.md
gitleaks detect --no-banner --redact --source .
```
