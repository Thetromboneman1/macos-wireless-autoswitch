# Deferred GitHub Stars Reassessment

Date: 2026-06-22

## Scope

This pass re-opened the prior GitHub-star artifacts and re-evaluated every item that had been treated as document-only, reference-only, trial-only, deferred, not installed, not downloaded, large-download, excessive-storage, resource-intensive, or manual-review work. Download size, storage, bandwidth, repository size, model size, dataset size, and asset size were removed as decision blockers.

Reviewed artifacts:

- `docs/autonomous-modernization/github-stars-approved-execution.json`
- `docs/autonomous-modernization/github-stars-implementation-plan.json`
- `docs/autonomous-modernization/11-github-stars-full-implementation.md`
- `docs/autonomous-modernization/12-approved-star-implementation.md`
- `docs/autonomous-modernization/deferred-star-checkout-inventory.json`
- `docs/autonomous-modernization/deferred-star-github-metadata.json`
- `docs/autonomous-modernization/deferred-star-manifest-validation.json`
- `docs/autonomous-modernization/deferred-star-install-feasibility.json`

## Result

All 25 previously limited candidates were shallow-cloned into ignored local checkouts under `tmp/star-downloads/` and reassessed with current GitHub metadata. No candidate remains documentation-only, reference-only, deferred, or trial-only solely because of download size, storage, bandwidth, model size, asset size, dataset size, or expected download duration.

Where installation did not proceed, the blocker is now explicitly one of: licensing/model terms, missing credentials, security boundary, platform incompatibility, global tool overlap, stale upstream packaging, or lack of a concrete integration target.

Host context used for platform checks: macOS 26.5.1, arm64.

## Validation Summary

| Check | Result |
|---|---|
| Candidate clone count | 25 local checkouts in `tmp/star-downloads/` |
| GitHub metadata refresh | 25/25 successful through `gh api` |
| Manifest parse validation | package/pyproject/Cargo manifests parsed; no parse failures |
| Lock/build dry checks | 11/13 passed; 2 actionable non-size failures |
| Remaining non-size failures | `StarTrail-org/LEANN` missing editable package path; `headroomlabs-ai/headroom` stale `uv.lock` |

## Reassessment Table

| Repository | Original classification | Original deferment reason | Size influenced? | New assessment | Installation status | Integration status | Validation results | Final recommendation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Aider-AI/aider | Document Only / Document Only | Potentially useful reference; no safe install path needed today. | Size was not the core blocker; previous doc-only call was mostly overlap with Codex/OpenCode. | Downloaded and technically assessable; mature Apache-2.0 Python CLI with local endpoint support, but duplicates the active Codex/OpenCode workflow. | Downloaded to tmp/star-downloads/Aider-AI__aider; pyproject parsed. Not globally installed because it would add a second coding-agent CLI without solving a current gap. | Documented as optional fallback coding CLI; no active wiring. | Clone/manifest validation passed; no lockfile dry check available in checkout. | Keep as optional manual fallback, not default stack. |
| Egonex-AI/Understand-Anything | Document Only / Create Trial Harness | Useful local-AI item but install or indexing/model assets need a scoped trial. | Partial. Prior trial limit referenced indexing large repos; that size/indexing concern is removed. | Strong Codex/OpenCode/Hermes code-understanding candidate; install script modifies agent/plugin surfaces and should be security-reviewed before execution. | Downloaded to tmp/star-downloads/Egonex-AI__Understand-Anything; package.json parsed. Not installed because installer/plugin symlink behavior changes global agent config. | Eligible for isolated one-repo code graph trial after script review. | Clone and manifest validation passed. | Run only in a disposable workspace first; size is no longer a blocker. |
| NPC-Worldwide/incognide | Document Only / Document Only | Useful local-AI item but install or indexing/model assets need a scoped trial. | Size was not the core blocker; prior doc-only status was overlap/app-scope risk. | Full local/cloud AI IDE with oMLX/llama.cpp support; useful for comparison, but it stores provider keys in user config and overlaps current tools. | Downloaded to tmp/star-downloads/NPC-Worldwide__incognide; npm dry-run passed. Not launched to avoid creating app state/provider-key config. | Documented as GUI/DX comparison candidate against Codex/OpenCode/LM Studio. | npm ci --ignore-scripts --dry-run passed. | Keep for isolated GUI trial, not default wiring. |
| OpenHands/OpenHands | Document Only / Document Only | Useful local-AI item but install or indexing/model assets need a scoped trial. | Partial. Prior trial language referenced runtime/assets; size removed, but full-filesystem access remains. | High-value agent workbench, actively maintained, but README warns local direct server gives full filesystem access. | Downloaded to tmp/star-downloads/OpenHands__OpenHands; uv lock check passed. Not started because agent-server filesystem access is a security boundary decision. | Potential Docker-sandboxed trial only. | uv lock --check passed. | Do not enable on host; evaluate only in sandboxed Docker/VM. |
| RyanCodrai/turbovec | Document Only / Document Only | Useful local-AI item but install or indexing/model assets need a scoped trial. | Partial. Prior trial concern was integration scope, not size. | Lightweight local vector-search library with LangChain/LlamaIndex/Haystack integrations; relevant to RAG experiments. | Downloaded to tmp/star-downloads/RyanCodrai__turbovec; Cargo metadata passed. Not globally installed because no current repo imports it yet. | Candidate for future LEANN/local-doc RAG benchmark. | cargo metadata --locked --no-deps passed. | Keep as active RAG implementation candidate. |
| RyjoxTechnologies/Octopoda-OS | Document Only / Document Only | Useful local-AI item but install or indexing/model assets need a scoped trial. | Partial. Prior trial concern mentioned local embedding extras; size removed. | Local agent memory, loop detection, audit trail, dashboard; useful but writes local state and overlaps existing memory conventions. | Downloaded to tmp/star-downloads/RyjoxTechnologies__Octopoda-OS; pyproject parsed. Not installed globally to avoid creating ~/.synrix state without a target agent. | Candidate for isolated local-mode agent observability trial. | Manifest validation passed. | Pilot with one disposable agent before adding to shared workflow. |
| StarTrail-org/LEANN | Document Only / Create Trial Harness | Useful local-AI item but install or indexing/model assets need a scoped trial. | Yes. Prior docs explicitly limited it to small docs subsets and approval before indexing large data. | Excellent private local RAG candidate and supports OpenAI-compatible llama.cpp endpoints; size blocker removed. | Downloaded to tmp/star-downloads/StarTrail-org__LEANN. Not installed because uv lock check currently fails on missing editable package path packages/astchunk-leann. | Still high-value; integration blocked by packaging issue, not data size. | uv lock --check failed: missing packages/astchunk-leann project path. | Track upstream packaging issue or install PyPI package in a clean env for next RAG benchmark. |
| TheStageAI/edge-lm | Needs Approval / Create Trial Harness | Useful local-AI item but install or indexing/model assets need a scoped trial. | Yes. Prior deferral was model/runtime download decision. | MLX-native Gemma edge model tooling; directly relevant to Ahmad-style MLX vs llama.cpp bake-off. | Downloaded to tmp/star-downloads/TheStageAI__edge-lm; pyproject parsed. Model weights not pulled because Gemma model terms/license acknowledgement remains a non-size gate. | Candidate for MLX benchmark once model terms are accepted and exact checkpoint is selected. | Manifest validation passed. | Proceed after license review; size is no longer a blocker. |
| affaan-m/ECC | Document Only / Document Only | Useful local-AI item but install or indexing/model assets need a scoped trial. | Size was not the core blocker; overlap/stacking risk was. | Large skill/command ecosystem with explicit security guidance; valuable as reference, but README/docs warn against unsafe agent patterns and duplicate current skill stack. | Downloaded to tmp/star-downloads/affaan-m__ECC; npm dry-run passed. Not installed because stacking skill frameworks can change agent behavior globally. | Use security guide and selected skills as reference, not a global install. | npm ci --ignore-scripts --dry-run passed. | Keep curated-reference only due behavior overlap, not size. |
| anysphere/priompt | Document Only / Document Only | Potentially useful reference; no safe install path needed today. | No. Prior rationale was prompt-library fit, not size. | Prompt JSX library; useful if building TypeScript prompt composition, but no current repo needs it. | Downloaded to tmp/star-downloads/anysphere__priompt; package metadata parsed where present. Not installed. | No integration target found. | Clone validation passed. | Ignore for now; no size blocker involved. |
| apple/container | Document Only / Download Needed Files | Mac utility candidate; no direct safe repo integration needed today. | Partial. Prior bucket wanted downloaded files; size is no longer a gate. | Apple container runtime is relevant to macOS isolation research, but Docker Desktop already exists and runtime replacement has operational implications. | Downloaded to tmp/star-downloads/apple__container; Swift package dump passed. Not installed because replacing/adding a container runtime affects host networking/storage. | Documented as future sandbox backend candidate for agents. | swift package dump-package passed. | Evaluate separately from Docker Desktop, not as part of star sweep. |
| beamivalice/PonyExl3 | Needs Approval / Create Trial Harness | Useful local-AI item but install or indexing/model assets need a scoped trial. | Yes. Prior approval gate covered model/runtime assets. | Apple Silicon/Metal inference experiment; relevant to local model evaluation. | Downloaded to tmp/star-downloads/beamivalice__PonyExl3; uv lock check passed. No models pulled because model choice/license remains unresolved. | Candidate for MLX/Metal model-format comparison. | uv lock --check passed. | Benchmark after selecting compatible model; size no longer blocks. |
| diegosouzapw/OmniRoute | Document Only / Download Needed Files | Useful local-AI item but install or indexing/model assets need a scoped trial. | Partial. Prior bucket wanted downloaded files; size removed. | Powerful provider/gateway router for Codex and OpenAI-compatible clients; high integration value but manages OAuth/API keys and could sit in front of every request. | Downloaded to tmp/star-downloads/diegosouzapw__OmniRoute; npm dry-run passed. Not launched because provider auth/routing security needs explicit design. | Candidate for isolated routing-lab, not production default. | npm ci --ignore-scripts --dry-run passed. | Do not replace direct local endpoint until threat model is complete. |
| go-gitea/gitea | Document Only / Document Only | Potentially useful reference; no safe install path needed today. | No. Prior rationale was mission fit, not size. | Excellent self-hosted Git forge, but GitHub remains the active remote and this sweep does not need a forge migration. | Downloaded to tmp/star-downloads/go-gitea__gitea; uv lock check passed for included Python tooling. Not installed. | No current integration. | uv lock --check passed. | Skip unless a self-hosted Git migration becomes an objective. |
| google-gemma/gemma-skills | Document Only / Download Needed Files | Useful local-AI item but install or indexing/model assets need a scoped trial. | Partial. Prior bucket wanted downloaded files; size not a blocker. | Relevant Gemma prompt/skill patterns; low-risk reference for Gemma role contract. | Downloaded to tmp/star-downloads/google-gemma__gemma-skills; no install needed. | Integrated as model/prompt research source for Gemma workflows. | Clone validation passed. | Keep as approved reference source; no runtime install exists. |
| headroomlabs-ai/headroom | Document Only / Create Trial Harness | Useful local-AI item but install or indexing/model assets need a scoped trial. | Partial. Prior trial concern was runtime/token proxy scope, not just size. | Strong prompt compression/proxy/MCP candidate, but it exchanges GitHub/Copilot tokens and Python lockfile is stale. | Downloaded to tmp/star-downloads/headroomlabs-ai__headroom; Rust metadata passed; uv lock check failed. Not enabled because token proxying is sensitive. | Potential compression/proxy benchmark after lockfile/security review. | cargo metadata passed; uv lock --check says uv.lock needs update. | High-value but gated by stale lockfile and token-boundary review, not size. |
| john-rocky/coreai-model-zoo | Needs Approval / Create Trial Harness | Useful local-AI item but install or indexing/model assets need a scoped trial. | Yes. Prior model download/runtime approval was size-like, but platform is the real blocker now. | Interesting Core AI model zoo, but README targets .aimodel on iOS 27/macOS 27 while host is macOS 26.5.1. | Downloaded to tmp/star-downloads/john-rocky__coreai-model-zoo. Not installed due platform/runtime floor. | Reference only until macOS/Xcode Core AI runtime catches up. | Clone validation passed; host detected macOS 26.5.1 arm64. | Defer for platform incompatibility, not size. |
| kennss/SiliconScope | Document Only / Download Needed Files | Useful local-AI item but install or indexing/model assets need a scoped trial. | Partial. Prior bucket requested downloaded files; size removed. | Native Apple Silicon monitor with memory bandwidth/ANE/media engine visibility; useful for benchmark observation. | Downloaded to tmp/star-downloads/kennss__SiliconScope; Swift package dump passed. Not installed because a GUI/app install is a user-session change, but it is safe to trial manually. | Recommended as benchmark observer tool. | swift package dump-package passed. | Install/use when running extended MLX vs llama.cpp bake-off. |
| kyegomez/OpenMythos | Document Only / Document Only | Potentially useful reference; no safe install path needed today. | No. Prior rationale was research/reference fit, not size. | Research-oriented docs/code; no clear installation benefit for current stack. | Downloaded to tmp/star-downloads/kyegomez__OpenMythos; pyproject parsed. | No active integration. | Manifest validation passed. | Keep as research only because of fit, not size. |
| nvk/llm-wiki | Document Only / Create Trial Harness | Useful local-AI item but install or indexing/model assets need a scoped trial. | Partial. Prior trial limited knowledge-base ingestion; size removed. | High-value local-AI knowledge base, now downloaded and suitable for repo docs/agent context. | Downloaded to tmp/star-downloads/nvk__llm-wiki; no install required for markdown knowledge base. | Use as local reference corpus for future LEANN/RAG benchmark. | Clone validation passed. | Approved for indexing once RAG engine is selected. |
| osaurus-ai/osaurus | Document Only / Document Only | Useful local-AI item but install or indexing/model assets need a scoped trial. | Partial. Prior trial concern was agent/runtime overlap, not size. | Native agent harness with local model orientation; overlaps current Codex/OpenCode/oMLX setup. | Downloaded to tmp/star-downloads/osaurus-ai__osaurus; Makefile present. Not started to avoid adding another always-on agent surface. | Candidate for side-by-side agent DX comparison only. | Clone validation passed. | Do not make default; compare in isolation. |
| pewdiepie-archdaemon/odysseus | Document Only / Document Only | Potentially useful reference; no safe install path needed today. | No. Prior rationale was existing local project overlap, not size. | Already represented by local Odysseus work; upstream is AGPL and actively moving. | Downloaded to tmp/star-downloads/pewdiepie-archdaemon__odysseus; npm dry-run passed. Not installed to avoid duplicating existing local stack. | Use as upstream reference for the existing Odysseus repo. | npm ci --ignore-scripts --dry-run passed. | Keep existing local implementation; pull ideas deliberately. |
| skyzer/deslop-the-copy | Document Only / Document Only | Potentially useful reference; no safe install path needed today. | No. Prior rationale was limited fit, not size. | Small Claude-style writing skill; no runtime impact and not central to local AI infra. | Downloaded to tmp/star-downloads/skyzer__deslop-the-copy. No install needed. | No integration target. | Clone validation passed. | Skip for infra modernization. |
| supertone-inc/supertonic | Document Only / Document Only | Mac utility candidate; no direct safe repo integration needed today. | No. Prior rationale was utility fit, not size. | TTS/audio utility, not central to Codex/local coding/model-serving stack. | Downloaded to tmp/star-downloads/supertone-inc__supertonic. Not installed. | No integration target. | Clone validation passed. | Skip unless audio/local voice work becomes a goal. |
| tw93/Pake | Document Only / Document Only | Mac utility candidate; no direct safe repo integration needed today. | No. Prior rationale was Mac utility fit, not size. | Useful webpage-to-desktop wrapper, but no current local-AI workflow needs a packaged web app. | Downloaded to tmp/star-downloads/tw93__Pake. Not installed/built. | No integration target. | Clone validation passed. | Skip unless packaging a local dashboard becomes a goal. |

## Candidates Promoted By Removing Size Gates

- `StarTrail-org/LEANN`: no longer limited to tiny docs subsets; blocker is now upstream packaging, not index size.
- `TheStageAI/edge-lm`: no longer blocked by model-weight size; next gate is Gemma terms and exact checkpoint choice.
- `beamivalice/PonyExl3`: no longer blocked by model/runtime asset size; next gate is compatible model selection.
- `john-rocky/coreai-model-zoo`: size no longer blocks, but macOS 27/Core AI runtime floor still does.
- `nvk/llm-wiki`: no longer held as external reference only; approved as future local RAG corpus.
- `Egonex-AI/Understand-Anything`: indexing size no longer blocks; installer/global-agent changes still require security review.

## Explicit Non-Size Blockers

| Blocker | Repositories | Reason |
|---|---|---|
| Security / full-filesystem agent boundary | `OpenHands/OpenHands`, `Egonex-AI/Understand-Anything`, `headroomlabs-ai/headroom`, `diegosouzapw/OmniRoute` | These can alter agent execution, proxy credentials, expose OAuth/API tokens, or grant broad filesystem access. |
| Licensing / model terms | `TheStageAI/edge-lm`, `beamivalice/PonyExl3` | Model terms and exact model selection must be accepted before pulling/runtime use. |
| Platform incompatibility | `john-rocky/coreai-model-zoo` | Current host is macOS 26.5.1; repo targets macOS/iOS 27 Core AI `.aimodel` workflows. |
| Upstream packaging issue | `StarTrail-org/LEANN`, `headroomlabs-ai/headroom` | Lock/package checks fail independently of size. |
| Tool overlap / no integration target | `Aider-AI/aider`, `go-gitea/gitea`, `tw93/Pake`, `supertone-inc/supertonic`, `skyzer/deslop-the-copy`, `anysphere/priompt`, `kyegomez/OpenMythos` | They do not improve the current local-AI/Codex/DNS workflow enough to justify global install. |

## Implementation Notes

- I did not run curl-to-shell installers, OAuth flows, model-license-gated downloads, destructive uninstallers, or host-wide agent/proxy services.
- I did run shallow clones, live metadata refresh, manifest parsing, package-manager dry checks, Swift package dump checks, and Cargo metadata checks where available.
- The downloaded repos live under ignored `tmp/star-downloads/`; this keeps the main repo clean while preserving local evidence for follow-up trials.
- The new generated evidence files are committed candidates under `docs/autonomous-modernization/` so this reassessment is repeatable.

## Next Safe Actions

1. Run a dedicated LEANN install from PyPI or wait for upstream to fix `packages/astchunk-leann`, then index `nvk/llm-wiki` and selected local docs.
2. Accept/review Gemma terms for `TheStageAI/edge-lm`, then include it in the MLX vs llama.cpp bake-off.
3. Install SiliconScope or use Activity Monitor/powermetrics during the next benchmark pass.
4. Threat-model OmniRoute/headroom/OpenHands before enabling any proxy, OAuth, Copilot-token, or full-filesystem agent workflows.

## Rollback

- Remove downloaded reassessment clones: `rm -rf tmp/star-downloads`
- Remove generated evidence/report files: `rm docs/autonomous-modernization/deferred-star-*.json docs/autonomous-modernization/14-deferred-stars-reassessment.md`
- No global services, LaunchAgents, package-manager global installs, model pulls, or credential stores were modified in this reassessment pass.
